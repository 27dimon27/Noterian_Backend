global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "whitecrowsoft-backend"
    static_configs:
      - targets: ["localhost:${SERVER_PORT}"]
    metrics_path: "/metrics"

  - job_name: "node-exporter"
    static_configs:
      - targets: ["localhost:${PROMETHEUS_PORT}"]
